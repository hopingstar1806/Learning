package main

import (
	"fmt"
	"math"
)
/*
 * You can also write
import "fmt"
import "math"
 * at each line but it will be better if we
 * put it in () since it is easier to read
 */

func main() {
	fmt.Printf("Now you have %g problems.\n", math.Sqrt(7))
}
